{
  "name": "roboco-plugins",
  "version": "0.1.0-alpha",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^3.2.1",
    "yaml": "^2.7.1",
    "@vitest/coverage-v8": "^3.2.1"
  }
}
